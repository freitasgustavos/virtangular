'use strict';

angular.module('newApp')

    .controller('RecuperarSenhaCtrl', ['$scope',

        function($scope) {

            $scope.$on('$viewContentLoaded', function() {

            });
        }
    ]);

